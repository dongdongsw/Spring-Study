	package com.sist.main;
	/*
	 * 	===> Container : 클래스를 관리하는 영역
	 * 			| bean에 등록된 모든 클래스(컴포넌트)
	 * 			| vo => 데이터형이기 때문에 등록하지 않는다
	 * 			 ------------
	 * 				BeanFactory : DI
	 * 					|
	 * 				ApplicationContext : DI / AOP
	 * 					| AnnotationConfigApplicationContext(어노테이션 사용시)
	 * 			WebApplicationContext : MVC
	 * 			==> 결합성이 낮은 프로그램
	 * 				(클래스를 수정 => 다른 클래스의 영향력)
	 * 			==> 상속 / 인터페이스 상속없이 사용(일반 클래스 : POJO)
	 * 												  -----
	 * 		
	 * 		1) DI => 값 설정하는 방법
	 * 				 ------------ setter DI / constructor DI
	 * 		2) 공통으로 사용되는 기능 : 자동화 처리 => AOP
	 * 			=> 트랜잭션 사용 / 로그 파일 ...
	 * 			=> MVC : 공통 예외처리
	 * 
	 * 		3) 스프링은 => 반복적인 코딩
	 * 		4) MVC => 1차 프로젝트와 동일
	 * 		  ----------------------
	 * 		5) 인증 / 인가 => 권한 부여 : security
	 * 		------------------------------------------------------
	 *		=> VueJS
	 * 		------------------------------------------------------
	 *  DL	DI	AOP	=> MVC
	 * 					|
	 * 					web개발 (첨부된 라이브러리)
	 * 			 |
	 * 			공통 모듈
	 * 		|
	 * 		객체 생성시에 필요한 멤버 변수의 값을 주입 (데이터베이스)
	 *   | 
	 *   클래스 찾기(getBean(id명))
	 *   동작 순서
	 *   ------
	 *   1. 객체 생성
	 *   	------ 라이브러리 => XML, Annotation
	 *   	<bean id="" class="" scope=""> : 클래스 한개 등록
	 *   	<context:component-scan basepackage="> :패키지 단위 등록
	 *   	----------------------------------------------------
	 *   	 id : 중복 없이 사용
	 *   	 class : 패키지부터 등록
	 *   	 scope : singleton / prototype
	 *   						    | clone => 객체 복제
	 *   				| 메모리를 한개만 사용 => 디폴트
	 *   2. 값을 주입
	 *   	=>  멤버 변수에만 주입이 가능]
	 *   		초기값 설정 : ORM(데이터베이스 연동 => 마이바티스)
	 *   	=> setter : setxxx (여기에 값 설정)
	 */
	public class MainClass {
	
		public static void main(String[] args) {
			// TODO Auto-generated method stub
	
		}
	
	}
